@model ChhayaNirh.Models.Report
<div style="border:1px solid #ccc; margin:10px; padding:10px;">
    <strong>@Model.ReportType</strong>
    <p>@Model.Description</p>
    <small>@Model.ReportedAt.ToString("dd MMM yyyy")</small>
</div>
